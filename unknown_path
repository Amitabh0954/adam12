Given the updated requirements for password recovery, we need to integrate password reset functionality with email verification, ensuring the reset links expire within 24 hours. Following the initial setup, we will add the necessary files and logic.

## Updated Folder Structure:
my_project/
├── backend/
│   ├── models/
│   │   ├── user_model.py
│   │   ├── password_reset_model.py
│   ├── views/
│   │   ├── user_view.py
│   ├── controllers/
│   │   ├── user_controller.py
│   │   ├── password_reset_controller.py
├── database/
│   ├── schema.sql
├── app.py
├── requirements.txt

## Updated User Stories:
4. Users can send a password reset request.
5. Users must verify their identity via email.
6. Password reset links must expire after 24 hours.

Given these, let's proceed step by step:

### Step 1: Add the `password_reset_model.py`.
### Step 2: Implement the `password_reset_controller.py` for password recovery.
### Step 3: Update `user_controller.py` to include login and invalid attempt handling.
### Step 4: Update the `schema.sql` to include the password reset table.
### Step 5: Update `requirements.txt` to include Flask-Mail.

Let's start with `password_reset_model.py`: